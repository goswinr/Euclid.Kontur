<?xml version="1.0" encoding="utf-8"?>
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Library</OutputType>
    <!-- FsDoc will use the first target framework: https://fsprojects.github.io/FSharp.Formatting/apidocs.html#Selected-projects -->
    <TargetFrameworks>net6.0;net472</TargetFrameworks>
    <LangVersion>preview</LangVersion>
    <NeutralLanguage>en</NeutralLanguage>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <SatelliteResourceLanguages>en</SatelliteResourceLanguages>    <!--to only have the english resources for FSharp.Core-->
    <RootNamespace>Euclid</RootNamespace>
    <AssemblyName>Euclid.Kontur</AssemblyName>
    <AssemblyTitle>Euclid.Kontur</AssemblyTitle>
    <Title>Euclid.Kontur</Title>
    <Product>Euclid.Kontur</Product>
    <PackageId>Euclid.Kontur</PackageId>    <!-- <Version></Version>  done by Ionide.KeepAChangelog.Tasks-->
    <Authors>GoswinR</Authors>
    <Copyright>Goswin Rothenthal 2026</Copyright>
    <Description>Boolean operations on Euclid 2D polygons: union, intersection, difference, xor, simplification and union of many shapes. Floating-point coordinates with an absolute tolerance, for .NET and Fable.</Description>
    <PackageDescription>Boolean operations on Euclid 2D polygons: union, intersection, difference, xor, simplification and union of many shapes. Floating-point coordinates with an absolute tolerance, for .NET and Fable.</PackageDescription>    <!--used on nuget.org-->
    <WarningLevel>5</WarningLevel>    <!-- WarningLevel 3 is default, see https://github.com/dsyme/fsharp-presentations/blob/master/design-notes/off-by-default-warnings.md -->
    <OtherFlags>$(OtherFlags) --warnon:3390</OtherFlags>    <!-- warnon:3390 is to verify xml docstrings https://devblogs.microsoft.com/dotnet/announcing-f-5/#improved-compiler-analysis-for-library-authors -->
    <OtherFlags>$(OtherFlags) --warnon:1182</OtherFlags>    <!-- 1182: warn on unused variables -->
  </PropertyGroup>

  <PropertyGroup>
    <PackageTags>fsharp;geometry;polygon;boolean;clipping;fable;fable-library</PackageTags>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <IncludeSymbols>true</IncludeSymbols>
    <PublishRepositoryUrl>true</PublishRepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageIcon>logo128.png</PackageIcon>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <DebugType>embedded</DebugType>
    <EmbedUntrackedSources>true</EmbedUntrackedSources>
    <RepositoryUrl>https://github.com/goswinr/Euclid.Kontur</RepositoryUrl>
    <FsDocsLicenseLink>https://github.com/goswinr/Euclid.Kontur/blob/main/LICENSE.md</FsDocsLicenseLink>
    <FsDocsReleaseNotesLink>https://github.com/goswinr/Euclid.Kontur/blob/main/CHANGELOG.md</FsDocsReleaseNotesLink>
    <PackageProjectUrl>https://goswinr.github.io/Euclid.Kontur</PackageProjectUrl>
    <FsDocsFaviconSource>img/favicon.ico</FsDocsFaviconSource>
    <FsDocsWarnOnMissingDocs>true</FsDocsWarnOnMissingDocs>
    <ChangelogFile>../CHANGELOG.md</ChangelogFile>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Update="FSharp.Core" Version="6.0.7" />
    <PackageReference Include="Euclid" Version="0.51.0" />
    <PackageReference Include="Fable.Core" Version="4.5.0" Condition="$(DefineConstants.Contains('FABLE_COMPILER'))" />    <!-- needed by the Fable sources of the Euclid package -->

    <!-- build time only : -->
    <PackageReference Include="Microsoft.SourceLink.GitHub" Version="10.0.400" PrivateAssets="all" IncludeAssets="runtime; build; native; contentfiles; analyzers" />
    <PackageReference Include="Ionide.KeepAChangelog.Tasks" Version="0.3.3" PrivateAssets="all" />

    <!-- the internal buffers and the BVH are tested directly: -->
    <InternalsVisibleTo Include="Test" />

    <None Include="../README.md" Pack="true" PackagePath="" />
    <None Include="../Docs/img/logo128.png" Pack="true" PackagePath="" />    <!--PackagePath = "" makes the file available to nuget pack tool-->
    <!--so that it can be used with Fable. Only Src, not the tests or the generated obj files.
        The Src items need their own PackagePath because MSBuild strips the part of the path before the wildcard.-->
    <Content Include="Euclid.Kontur.fsproj" PackagePath="fable/Src/" />
    <Content Include="**/*.fs; **/*.fsi" Exclude="bin/**;obj/**" PackagePath="fable/Src/" />

    <Compile Include="FillRule.fs" />
    <Compile Include="Kontur.fs" />
    <Compile Include="Arr.fs" />
    <Compile Include="Buffers.fs" />
    <Compile Include="Bvh.fs" />
    <Compile Include="EngineState.fs" />
    <Compile Include="Ingest.fs" />
    <Compile Include="Intersect.fs" />
    <Compile Include="Graph.fs" />
    <Compile Include="Winding.fs" />
    <Compile Include="Link.fs" />
    <Compile Include="Engine.fs" />
    <Compile Include="KonturModule.fs" />
  </ItemGroup>

  <Target Name="CopyReadmeToDocs" AfterTargets="Build">    <!-- for fsdocs to build locally -->
    <Copy SourceFiles="../README.md" DestinationFiles="../Docs/index.md" ContinueOnError="false" />
  </Target>

  <Target Name="WriteChangelogVersion" AfterTargets="AfterBuild">    <!-- for checking that the git tag matches the Changelog during github tag based builds -->
    <WriteLinesToFile File="../bin/ChangelogVersion.txt" Lines="@(CurrentReleaseChangelog)" Overwrite="true" ContinueOnError="false" />
  </Target>

</Project>
